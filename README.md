# Narakim-Roblox-bridge
-- Place this Script in ServerScriptService. Enable HttpService (Game Settings → Security). -- Create an experience secret named NARAKIM_BRIDGE_TOKEN matching ROBLOX_BRIDGE_SECRET in Narakim. -- Set Config.BridgeUrl to https://engine.narakim.cloud/api/webhooks/roblox/bridge/&lt;botId>
